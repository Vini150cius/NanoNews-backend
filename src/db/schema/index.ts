import { articles } from "./articles";
import { content_of_article } from "./content_of_article";
import { users } from "./users";

export const schema = {
  users,
  articles,
  content_of_article,
};
